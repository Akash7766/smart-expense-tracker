# Production API origin (no code changes needed — swap before deploy).
# Routes are mounted under /api on the backend; /api is appended automatically if missing.
VITE_API_URL=https://your-backend-url.onrender.com
